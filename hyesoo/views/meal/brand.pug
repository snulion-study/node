doctype html
html
    head
        meta(charset='utf-8')
        style
            include ../../css/index.css
    body
        div.title
                a(href='/meal') 오늘은 머멍냥
        each meal in meals
            a(href='/meal/show/'+meal._id)
                div.meal
                    p.mealbrand= meal.brand.name
                    p.mealname= meal.title
                    p.description= meal.description
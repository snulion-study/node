doctype html
html
    head
        meta(charset='utf-8')
        style
            include ../../css/index.css
    body
        div.title
                a(href='/meal') 오늘은 머멍냥

        form(action='/meal/update/'+meal._id method='post')
            p
                input(type='text' name='title' value=meal.title)
            p
                textarea(name='description')= meal.description
            p
                input(type='submit')
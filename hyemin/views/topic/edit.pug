doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Server Side JavaScript

        form(action='/topic/update/'+topic._id method='post')
            p
                input(type='text' name='title' value=topic.title)
            p
                textarea(name='description')= topic.description
            p
                input(type='submit')